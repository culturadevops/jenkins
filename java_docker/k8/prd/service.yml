apiVersion: v1
kind: Service
metadata:
  labels:
    app: us-ordermanager
  name: us-ordermanager
spec:
  type: NodePort
  ports:
  - name: "80"
    port: 80
    targetPort: 8031
  selector:
    app: us-ordermanager
status:
  loadBalancer: {}
